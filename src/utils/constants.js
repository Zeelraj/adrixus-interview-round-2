const PASSWORD_LENGTH = 6;

const TABLE_HEADERS = ["ID", "Name", "Email", "Contact", "Gender"];

const USER_PER_PAGE = 10;

const FILTER_PARAMS = [
  "id",
  "firstName",
  "lastName",
  "email",
  "contact",
  "gender",
];

export { PASSWORD_LENGTH, TABLE_HEADERS, USER_PER_PAGE, FILTER_PARAMS };
